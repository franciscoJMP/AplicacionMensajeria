<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplicaciondemensajeria">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconofinal"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconofinal"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplicacionDeMensajeria">
        <activity android:name=".ActualizarDatos"></activity>
        <activity android:name=".Registro" />
        <activity
            android:name=".ventanaPrincipal"
            android:label="@string/title_activity_ventana_principal"
            android:theme="@style/Theme.AplicacionDeMensajeria.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>